numpy>=1.26
scipy>=1.13
pretty_midi>=0.2.10
mido>=1.3
matplotlib>=3.8
pytest>=8.0
mir_eval>=0.7
# Figure 1 (staff-notation engraving), used only by experiments/figs/fig1_two_scenario.py:
verovio>=6.0        # MusicXML -> SVG engraving; pure wheel, no system deps
svglib>=1.5         # SVG -> PDF (vector), pure Python
reportlab>=4.0      # PDF backend for svglib
pymupdf>=1.24       # compose panels/labels into the final PDF; strip unembedded fonts
